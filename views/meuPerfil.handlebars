<h1 class="text-center mt-5">Perfil</h1>
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <p class="text-center mt-5">{{usuario.nome}}</p>
            <p class="text-center">{{usuario.email}}</p>
            <p class="text-center">{{usuario.tipoUsuario}}</p>
        </div>
    </div>
</div>
